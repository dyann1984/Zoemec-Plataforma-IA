import test from 'node:test';import assert from 'node:assert/strict';import {makeEmptyAPUv2} from './apuSchema.js';import {auditChange,createApuVersion,restoreApuVersion,comparePrice} from './apuVersioning.js';
test('versionado conserva snapshots y restaurar crea una version nueva',()=>{const a=makeEmptyAPUv2();a.concept='APU';a.unit='m2';a.labor=[{cantidad:1,salarioBase:100,fsr:1,fuente:{}}];const v1=createApuVersion(a,[],{at:'2026-01-01',user:'Ana'});v1.apu.labor[0].salarioBase=120;const v2=createApuVersion(v1.apu,v1.history,{at:'2026-01-02'});const restored=restoreApuVersion(v1.history[0],v2.history,{at:'2026-01-03'});assert.deepEqual(restored.history.map(v=>v.version),['V1','V2','V3']);assert.equal(restored.apu.labor[0].salarioBase,100);});
test('auditoria registra antes y despues y comparador calcula variacion',()=>{const log=auditChange([],{user:'Ana',field:'precio',before:4.2,after:4.7,at:'x'});assert.equal(log.length,1);assert.ok(Math.abs(comparePrice(4.2,4.7).variationPct-11.9047619)<1e-6);});
